<!doctype html>
<html>
    <head>
        <title><%= title %></title>
        <link rel="stylesheet" href="/style.css"/>
    </head>
    <body>
        <div class='category-list'>
            <div class='category'>
            <div class="title-section"><h4><%=title%></h4></div>
            <form method="POST">
                <div class="form-row">
                <label for="name">Developer name: </label>
                <input name="name" type="text" value="<%=developer.name || ''%>"></input>
                </div>
                <div class="form-row">
                <label for="country">Developer country: </label>
                <input name="country" type="text" value="<%=developer.country || ''%>"></input>
                </div>
                <%if (locals.errors) {%>
                <div class="errors-section section">
                    <%errors.forEach((error) => {%>
                        <p><%= error.msg%></p>
                    <%})%>

                </div>
                <%}%>
                <button>Update developer</button>
            </form>
            </div>
        </div>
    </body>
</html>

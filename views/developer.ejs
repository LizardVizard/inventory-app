<!doctype html>
<html>
    <head>
        <title><%= title %></title>
        <link rel="stylesheet" href="/style.css"/>
    </head>
    <body>
        <%if (!developerFound) {%>
            <h3>Developer not found</h3>
        <%} else {%>
            <h3> <%= developer.name %> from <%= developer.country%> </h3>
            <h4>Games developed:</h4>
            <ul>
                <%if (!games) {%>
                    <b>No games</b>
                <%} else {%>
                    <%games.forEach((game) => {%>
                        <li><a href="/games/<%=game.id%>"><%= game.title%></a></li>
                    <%})%>
                <%}%>
            </ul>
        <%}%>
    </body>
</html
